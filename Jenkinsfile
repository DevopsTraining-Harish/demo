pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'this is build job'
            }
        }
        stage('Test') {
            steps {
                echo 'this is test job'
            }
        }
        stage('Deploy') {
            steps {
                echo 'this is deploy job'
            }
        }
    }
}
